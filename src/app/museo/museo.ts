import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-museo',
  imports: [RouterLink],
  templateUrl: './museo.html',
  styleUrl: './museo.css',
})
export class Museo {}
